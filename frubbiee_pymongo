import os
import pymongo
if os.path.exists("env.py"):
       import env


MONGO_URI = os.environ.get("MONGO_URI")
DATABASE = "frubbie_drinks"
COLLECTION = "users"


def mongo_connect(url):
    try:
        pass
    except expression as identifier:
        pass
    else:
        pass
    finally:
        pass
conn = mongo_connect(MONGO_URI)

coll = conn[DATABASE][COLLECTION]
 
print(doc)
